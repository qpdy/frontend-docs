# Use the public registry
registry=https://registry.npmjs.org/

# Enable strict peer dependencies check
strict-peer-dependencies=true

# Enable hoisting to reduce duplication
hoist=true

# Save exact versions
save-exact=false